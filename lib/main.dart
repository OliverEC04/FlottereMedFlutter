import 'package:flutter/material.dart';

void main() {
  runApp(const HejDerWidget());
}

class HejDerWidget extends StatelessWidget {
  const HejDerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Container(
          child: const Text('dadadadwewqwefwrtg'),
        ),
      ),
    );
  }
}
